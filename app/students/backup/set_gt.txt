            
    emotions = []
    emotions_new_nodes = []
    tmp_emotions = Emotion.query.filter(Emotion.hide == False).filter(Emotion.used == True).all()
    for s in tmp_emotions:
        ts = set_gt_node(s, tmp_thoughts, "false")
        if ts != 0:
            emotions.append( ts )
            emotions_new_nodes.append( set_new_emty_gt_node(s, "true") )
            gts_arr.append( emotions )
            gts_arr.append( emotions_new_nodes )
        